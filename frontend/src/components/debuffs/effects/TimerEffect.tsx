// Логика этого эффекта живёт в Game.tsx:
// таймер в header показывается умноженным на 3 (только визуально).
// Компонент не рендерит ничего — эффект чисто в Game.tsx.
export function TimerEffect() {
  return null;
}
