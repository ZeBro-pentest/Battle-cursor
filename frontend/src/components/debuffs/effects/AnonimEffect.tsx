// Логика этого эффекта живёт в Game.tsx:
// при рендере карточек игроков username → "???", cursor → null.
// Компонент не рендерит ничего визуального сам по себе.
export function AnonimEffect() {
  return null;
}
